<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<fontconfig>

	<!-- This config file is used to customize access to fonts -->
	<!-- https://www.freedesktop.org/wiki/Software/fontconfig/ -->
	<!-- Set the envvar FONTCONFIG_PATH to the directory containing this file -->
	<!-- When run through the web server, this should happen automatically -->
	<dir prefix="relative">fonts/</dir>
	<dir prefix="xdg">fonts/</dir>
	<dir prefix="cwd">fontconfig/fonts/</dir>
</fontconfig>
